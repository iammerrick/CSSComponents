{{=<% %>=}} 
import React from 'react';

const <%name%> = ({<%args%>}) => (
  <div>
    <style dangerouslySetInnerHTML={{__html: "<%css%>"}}></style>
    <%={{ }}=%>
    {{{template}}}
  </div>
);

export default {{name}};
